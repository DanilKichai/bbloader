#!/usr/bin/env bash

build() {
    command cp --recursive /target/* "${BUILDROOT}"
    command mkdir --parents "${BUILDROOT}/archshell/efivars"
}

help() {
    cat <<HELPEOF
This hook provides target files.
HELPEOF
}
